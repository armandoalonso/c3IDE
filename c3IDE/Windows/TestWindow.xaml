<UserControl x:Class="c3IDE.Windows.TestWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:c3IDE.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Century Gothic" FontSize="16">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" LastChildFill="True" VerticalAlignment="Top">
            <TextBox Name="LogText" TextWrapping="Wrap" Height="700" DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto"/>
        </DockPanel>


        <StackPanel Grid.Column="1">
            <Button Name="StartAndTestButton" Content="Test C3 Addon" Margin="10" Click="TestC3AddonButton_Click" ></Button>
            <Button Name="StopWebServerButton" Content="Stop Web Server" Margin="10" Click="StopWebServerButton_Click" IsEnabled="False"></Button>
            <Button Name="OpenCompiledFOlder" Content="Open Compiled Folder" Margin="10" Click="OpenCompiledFolderButton_Click" ></Button>
        </StackPanel>
    </Grid>
</UserControl>
