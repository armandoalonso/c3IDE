"use strict";
{
     C3.Plugins.{{plugin.company}}_{{plugin.name}}.Instance = class {{plugin.name}}Instance extends C3.SDKInstanceBase
     {
          constructor(inst, properties)
          {
               super(inst);
               
               if (properties)          // note properties may be null in some cases
               {
                    //todo: add property here
               }
          }
          
          Release()
          {
               super.Release();
          }
          
          SaveToJson()
          {
               return {
                    // data to be saved for savegames
               };
          }
          
          LoadFromJson(o)
          {
               // load state for savegames
          }
     };
}