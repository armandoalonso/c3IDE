{
     "languageTag": "en-US",
     "fileDescription": "Strings for {{plugin.name}}",
     "text": {
          "plugins": {
               "{{plugin.company | string.downcase}}_{{plugin.classname | string.downcase}}": {
                    "name": "{{plugin.name}}",
                    "description": "{{plugin.description}}",
                    "help-url": "{{plugin.documentation}}",
                    "properties": {
                    },
                    "aceCategories": {
                    },
                    "conditions": {
                    },
                    "actions": {
                    },
                    "expressions": {
                    }
               }
          }
     }
}