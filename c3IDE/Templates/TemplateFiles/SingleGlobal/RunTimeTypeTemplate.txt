"use strict";

{
    C3.Plugins.{{plugin.company}}_{{plugin.name}}.Type = class {{plugin.name}}Type extends C3.SDKTypeBase
    {
        constructor(objectClass)
        {
            super(objectClass);
        }
        
        Release()
        {
            super.Release();
        }
        
        OnCreate()
        {	
            
        }
    };
}